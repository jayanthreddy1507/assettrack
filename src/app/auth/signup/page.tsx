import { AuthCard, SignupForm } from "@/components/auth";

export default function SignupPage() {
  return (
    <AuthCard title="Create your account" subtitle="Join AssetFlow as an employee">
      <SignupForm />
    </AuthCard>
  );
}
